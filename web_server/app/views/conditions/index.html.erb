<p id="notice"><%= notice %></p>

<h1>Listing Conditions</h1>

<table>
  <thead>
    <tr>
      <th>Condition id</th>
      <th>Condition effect</th>
      <th>Condition info</th>
      <th>Percent</th>
      <th>Condition apply type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @conditions.each do |condition| %>
      <tr>
        <td><%= condition.CONDITION_ID %></td>
        <td><%= condition.CONDITION_EFFECT %></td>
        <td><%= condition.CONDITION_INFO %></td>
        <td><%= condition.PERCENT %></td>
        <td><%= condition.CONDITION_APPLY_TYPE %></td>
        <td><%= link_to 'Show', condition %></td>
        <td><%= link_to 'Edit', edit_condition_path(condition) %></td>
        <td><%= link_to 'Destroy', condition, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Condition', new_condition_path %>
